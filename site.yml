---
- name: Debian-based linux System Hardening
  hosts: test_nodes
  become: true
  vars_files:
    - group_vars/all.yml
    - group_vars/all_secrets.yml
  roles:
    - common_handlers
    - filesystem
    - package_mgmt
    - access_control
    - kernel_boot
    - network_firewall
    - auth_ssh
